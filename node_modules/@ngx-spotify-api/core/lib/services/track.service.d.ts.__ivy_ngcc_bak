import { HttpClient } from '@angular/common/http';
import { Observable } from 'rxjs';
import { Track } from '../models';
import { AudioFeatures } from '../models';
import { AudioAnalysis } from '../models';
import { ApiConfig } from '../models';
export declare class TrackService {
    private config;
    private http;
    constructor(config: ApiConfig, http: HttpClient);
    getTrack(id: string, market?: string): Observable<Track>;
    getAudioFeatures(id: string): Observable<AudioFeatures>;
    getAudioAnalysis(id: string): Observable<AudioAnalysis>;
    getTracks(ids: string[], market?: string): Observable<Track[]>;
    getSeveralAudioFeatures(ids: string[]): Observable<AudioFeatures[]>;
}
