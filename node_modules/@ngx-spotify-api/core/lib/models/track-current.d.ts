import { Context } from './context';
import { Track } from './track';
export declare class TrackCurrent {
    context: Context;
    timestamp: number;
    progressMs: number;
    isPlaying: boolean;
    currentlyPlayingType: 'track';
    item: Track;
}
